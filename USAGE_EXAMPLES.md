# 使用示例文档

本文档展示了指令查询插件 v2.0 的实际使用场景和效果。

## 📖 目录

- [LLM 工具调用示例](#llm-工具调用示例)
- [测试命令示例](#测试命令示例)
- [实际对话场景](#实际对话场景)

---

## LLM 工具调用示例

### 1. search_command - 搜索指令

#### 场景：用户输错指令

**用户输入：** `/钩鱼`

**LLM 思考过程：**
```
用户输入了 /钩鱼，但这个指令可能不存在。
让我搜索一下相关的指令。
```

**LLM 调用工具：**
```python
search_command(keyword="钩鱼")
```

**工具返回：**
```json
{
  "success": true,
  "message": "找到 1 条与 '钩鱼' 相关的指令",
  "results": [
    {
      "command": "/钓鱼",
      "description": "开始钓鱼游戏",
      "plugin": "钓鱼游戏插件",
      "aliases": ["/fishing", "/fish"]
    }
  ]
}
```

**LLM 回复用户：**
```
姐姐，是 /钓鱼 哦，不是钩鱼～
你也可以用别名 /fishing 或 /fish
```

---

#### 场景：用户找功能

**用户输入：** `有抽奖功能吗`

**LLM 调用工具：**
```python
search_command(keyword="抽奖")
```

**工具返回：**
```json
{
  "success": true,
  "message": "找到 3 条与 '抽奖' 相关的指令",
  "results": [
    {
      "command": "/抽奖",
      "description": "参与抽奖活动",
      "plugin": "抽奖插件",
      "aliases": ["/lottery"]
    },
    {
      "command": "/创建抽奖",
      "description": "创建新的抽奖活动",
      "plugin": "抽奖插件",
      "aliases": ["/create_lottery"]
    },
    {
      "command": "/我的抽奖",
      "description": "查看参与的抽奖记录",
      "plugin": "抽奖插件",
      "aliases": ["/my_lottery"]
    }
  ]
}
```

**LLM 回复用户：**
```
有的！抽奖功能包括：
• /抽奖 - 参与抽奖活动
• /创建抽奖 - 创建新的抽奖活动
• /我的抽奖 - 查看参与记录

快来试试吧～
```

---

### 2. get_command_detail - 查询指令详情

#### 场景：用户询问用法

**用户输入：** `钓鱼怎么玩`

**LLM 调用工具：**
```python
get_command_detail(command_name="钓鱼")
```

**工具返回：**
```json
{
  "success": true,
  "command": "/钓鱼",
  "description": "开始钓鱼游戏",
  "plugin": "钓鱼游戏插件",
  "aliases": ["/fishing", "/fish"],
  "similar_commands": ["/我的背包", "/鱼类图鉴", "/钓鱼排行"]
}
```

**LLM 回复用户：**
```
钓鱼游戏很简单：
使用 /钓鱼 开始钓鱼

你还可以：
• /我的背包 - 查看你钓到的鱼
• /鱼类图鉴 - 查看所有鱼类
• /钓鱼排行 - 查看排行榜

也可以用别名 /fishing 或 /fish 哦～
```

---

### 3. list_plugin_commands - 列举插件指令

#### 场景：查看所有插件

**用户输入：** `有哪些插件`

**LLM 调用工具：**
```python
list_plugin_commands(plugin_name="")
```

**工具返回：**
```json
{
  "success": true,
  "message": "系统共有 15 个插件",
  "plugins": [
    "钓鱼游戏插件",
    "抽奖插件",
    "表情包生成器",
    "群管理插件",
    "帮助菜单插件",
    "..."
  ],
  "hint": "使用 list_plugin_commands 并指定 plugin_name 参数查看具体插件的指令"
}
```

**LLM 回复用户：**
```
系统共有 15 个插件：
• 钓鱼游戏插件
• 抽奖插件
• 表情包生成器
• 群管理插件
• 帮助菜单插件
• ...

想了解某个插件的功能？告诉我插件名称吧～
```

---

#### 场景：查看插件功能

**用户输入：** `抽奖插件有什么功能`

**LLM 调用工具：**
```python
list_plugin_commands(plugin_name="抽奖")
```

**工具返回：**
```json
{
  "success": true,
  "plugin": "抽奖插件",
  "command_count": 5,
  "commands": [
    {
      "command": "/抽奖",
      "description": "参与抽奖活动",
      "aliases": ["/lottery"]
    },
    {
      "command": "/创建抽奖",
      "description": "创建新的抽奖活动",
      "aliases": ["/create_lottery"]
    },
    {
      "command": "/我的抽奖",
      "description": "查看参与的抽奖记录",
      "aliases": ["/my_lottery"]
    },
    {
      "command": "/抽奖列表",
      "description": "查看进行中的抽奖",
      "aliases": ["/lottery_list"]
    },
    {
      "command": "/结束抽奖",
      "description": "结束并开奖",
      "aliases": ["/end_lottery"]
    }
  ]
}
```

**LLM 回复用户：**
```
抽奖插件有 5 个功能：

1. /抽奖 - 参与抽奖活动
2. /创建抽奖 - 创建新的抽奖活动
3. /我的抽奖 - 查看参与记录
4. /抽奖列表 - 查看进行中的抽奖
5. /结束抽奖 - 结束并开奖

每个指令都有对应的英文别名，比如 /lottery、/create_lottery 等～
```

---

## 测试命令示例

### 1. 搜索指令测试

```bash
/测试指令搜索 钓鱼
```

**输出：**
```
🔍 搜索 '钓鱼' 的结果：

1. /钓鱼
   📦 插件: 钓鱼游戏插件
   📝 描述: 开始钓鱼游戏
   🔗 别名: /fishing, /fish

2. /我的背包
   📦 插件: 钓鱼游戏插件
   📝 描述: 查看你的渔获
   🔗 别名: /bag

3. /鱼类图鉴
   📦 插件: 钓鱼游戏插件
   📝 描述: 查看所有鱼类信息
   🔗 别名: /fish_book
```

---

### 2. 查询详情测试

```bash
/测试指令详情 钓鱼
```

**输出：**
```
📋 指令详情

🎯 指令: /钓鱼
📦 插件: 钓鱼游戏插件
📝 描述: 开始钓鱼游戏
🔗 别名: /fishing, /fish

💡 相关指令:
  • /我的背包
  • /鱼类图鉴
  • /钓鱼排行
```

---

### 3. 插件列表测试

```bash
# 查看所有插件
/测试插件列表
```

**输出：**
```
📦 系统插件列表 (15 个)

  • 钓鱼游戏插件
  • 抽奖插件
  • 表情包生成器
  • 群管理插件
  • 帮助菜单插件
  • ...

💡 使用 /测试插件列表 <插件名> 查看插件的指令
```

```bash
# 查看特定插件
/测试插件列表 钓鱼
```

**输出：**
```
📦 钓鱼游戏插件
共 5 条指令

• /钓鱼
  开始钓鱼游戏
  别名: /fishing, /fish

• /我的背包
  查看你的渔获
  别名: /bag

• /鱼类图鉴
  查看所有鱼类信息
  别名: /fish_book

• /钓鱼排行
  查看钓鱼排行榜
  别名: /fish_rank

• /卖鱼
  出售渔获换取金币
  别名: /sell_fish
```

---

## 实际对话场景

### 场景 1：新手用户探索

**对话流程：**

```
👤 用户: 这个机器人有什么功能啊
🤖 LLM: [调用 list_plugin_commands()]
🤖 LLM: 系统有很多功能呢！主要包括：
       • 钓鱼游戏插件 - 休闲小游戏
       • 抽奖插件 - 参与抽奖活动
       • 表情包生成器 - 制作表情包
       • 群管理插件 - 群组管理工具
       想了解哪个插件的详细功能？

👤 用户: 钓鱼游戏怎么玩
🤖 LLM: [调用 list_plugin_commands("钓鱼")]
🤖 LLM: 钓鱼游戏很有趣！你可以：
       • /钓鱼 - 开始钓鱼
       • /我的背包 - 查看渔获
       • /鱼类图鉴 - 浏览鱼类
       • /钓鱼排行 - 查看排名
       • /卖鱼 - 卖鱼赚钱
       快来试试 /钓鱼 吧～
```

---

### 场景 2：用户输入错误

**对话流程：**

```
👤 用户: /钩鱼
🤖 LLM: [调用 search_command("钩鱼")]
🤖 LLM: 姐姐，是 /钓鱼 哦，不是钩鱼～
       也可以用 /fishing 或 /fish

👤 用户: /钓鱼
🤖 系统: [执行钓鱼指令]
```

---

### 场景 3：功能咨询

**对话流程：**

```
👤 用户: 能不能生成表情包
🤖 LLM: [调用 search_command("表情包")]
🤖 LLM: 可以的！有表情包生成器插件：
       • /制作表情包 - 生成自定义表情包
       • /表情包模板 - 查看可用模板
       • /我的表情包 - 查看创作历史
       试试 /制作表情包 吧～

👤 用户: 怎么用
🤖 LLM: [调用 get_command_detail("/制作表情包")]
🤖 LLM: 使用方法很简单：
       /制作表情包 <模板名> <文字>

       例如：/制作表情包 为所欲为 我说的

       相关功能：
       • /表情包模板 - 查看所有模板
       • /我的表情包 - 查看历史
```

---

## 最佳实践

### 1. LLM 调用策略

- **首次接触**：用户问"有什么功能"时，调用 `list_plugin_commands()` 获取概览
- **功能探索**：用户问某个插件时，调用 `list_plugin_commands(plugin_name)` 获取详细列表
- **错误纠正**：用户输入不存在的指令时，调用 `search_command()` 查找相似指令
- **用法指导**：用户问怎么用时，调用 `get_command_detail()` 获取详细信息

### 2. 返回数据处理

- 始终检查 `success` 字段
- 失败时查看 `suggestions` 给出建议
- 成功时提取核心信息简洁回复
- 适当使用 emoji 让回复更友好

### 3. 用户体验优化

- 纠正错误时语气温和友善
- 推荐功能时给出具体示例
- 回复简洁明了，避免信息过载
- 主动推荐相关功能

---

## 总结

指令查询插件 v2.0 让 LLM 真正成为了用户的智能助手：

✅ **精准纠错** - 用户输错立即纠正
✅ **智能引导** - 不会用就教会用
✅ **功能发现** - 让用户了解更多功能
✅ **体验优化** - 让交互更自然流畅

通过合理使用三个核心工具函数，LLM 可以在各种场景下为用户提供帮助！
